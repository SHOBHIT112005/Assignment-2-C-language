#include<stdio.h>
int main()
{
    char ch;
    printf("Enter a character\n");
    scanf("%s",&ch);
    printf("ASCII code of character ch is %d",ch);//ASCII Code is printed whena character is printed with %d for mat specifier
    return 0;
}